ユーザーには日本語で応答してください。

- Python 製ツールの一時的な利用には `uvx` を使用する（例: `uvx pymupdf ...`）
- Homebrew パッケージは `brew/Brewfile` で管理する。直接 `brew install` しない

### Python開発環境
- `pip` / `pip install` は使用しない
- パッケージ管理には `uv add`、スクリプト実行には `uv run` を使用する
